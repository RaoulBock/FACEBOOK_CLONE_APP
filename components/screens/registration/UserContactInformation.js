import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UserContactInformation = () => {
  return (
    <View>
      <Text>UserContactInformation</Text>
    </View>
  );
};

export default UserContactInformation;

const styles = StyleSheet.create({});
